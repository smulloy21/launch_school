# Write a function named combine that takes three positional arguments and
# returns a tuple containing all three. Call this function with three
# different values.

def combine(a, b, c):
    return (a, b, c)

print(combine('one', 'last', 'thing')) # ('one', 'last', 'thing')
